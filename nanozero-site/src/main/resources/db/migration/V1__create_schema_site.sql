-- Le schéma 'site' est créé par Flyway lui-même (spring.flyway.schemas=site).
-- Migration no-op explicite : trace la création du schéma dans l'historique Flyway.
-- Les tables suivent en V2.
SELECT 1;
